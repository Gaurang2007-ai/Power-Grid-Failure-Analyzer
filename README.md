# Power-Grid-Failure-Analyzer
The Power Grid Failure Analyzer simulates how failures spread in a network of connected power stations using Depth First Search (DFS). It models the grid as a graph and identifies all affected stations when one fails. The project demonstrates how DFS can be applied to analyze real world network failures and system vulnerabilities.
